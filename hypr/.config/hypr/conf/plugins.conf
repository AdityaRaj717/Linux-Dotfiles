# Plugins: hyprscrolling + hyprexpo are configured but NOT installed/enabled
# (hyprpm list shows none enabled; active layout is dwindle).
# Hyprland 0.56 mis-parses plugin blocks for unloaded plugins and logs spurious
# config errors ("Invalid value X for finger count" / "Invalid direction").
# Install + enable the plugins, then uncomment this block to use it.
# plugin {
#     hyprscrolling {
#         column_width = 0.7
#         fullscreen_on_one_column = true
#     }
#     hyprexpo {
#         columns = 3
#         gap_size = 10
#         bg_col = rgb(111111)
#         workspace_method = first 1
#         enable_gesture = true
#         gesture_fingers = 3
#         gesture_distance = 300
#         gesture_positive = true
#     }
# }
